'use strict'

const webpack = require('webpack')
const merge = require('webpack-merge')
const base = require('./webpack.config.base')

module.exports = merge(base, {
  entry: [
    './index.js'
  ],
  output: {
    filename: 'marky-marked.min.js'
  },
  plugins: [
    new webpack.optimize.UglifyJsPlugin({
      sourceMap: false
    })
  ]
})
