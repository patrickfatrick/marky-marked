import { terser } from 'rollup-plugin-terser';
import base from './rollup.config.base';

export default Object.assign(base, {
  output: {
    file: 'dist/marky-marked.min.js',
    format: 'umd',
    name: 'markymark',
  },
  plugins: base.plugins.concat(terser()),
});
