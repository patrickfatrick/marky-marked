machine:
  node:
    version: 5
dependencies:
  pre:
    - npm clear cache
    - npm install npm@'>=3.3.6' -g
test:
  post:
    - cat ./coverage/**/lcov.info | ./node_modules/coveralls/bin/coveralls.js
