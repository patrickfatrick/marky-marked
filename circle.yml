machine:
  node:
    version: 5
dependencies:
  pre:
    - npm cache clear
    - npm install npm@'>=3.7.2' -g
test:
  post:
    - cat ./coverage/**/lcov.info | ./node_modules/codecov/bin/codecov
general:
  artifacts:
    - ./coverage
